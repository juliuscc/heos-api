const system = require('./commands/systemCommands')
const player = require('./commands/playerCommands')
const group = require('./commands/groupCommands')
const browse = require('./commands/browseCommands')

module.exports = { system, player, group, browse }
