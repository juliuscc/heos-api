const system = require('./systemCommands')
const player = require('./playerCommands')
const group = require('./groupCommands')
const browse = require('./browseCommands')

module.exports = { system, player, group, browse }
