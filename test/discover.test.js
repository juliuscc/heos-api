const discover = require('../lib/discover')

it('has no tests')

// it('Returns a promise', () => {
// 	expect(discover()).toBeInstanceOf(Promise)
// })

// it('Discovers a unit that is not null', () => {
// 	expect.assertions(1)
// 	return discover().then(devices => expect(devices).toBeDefined())
// })

// it('Discovers unit and return a unit', () => {
// 	expect.assertions(4)
// 	return discover().then(devices => {
// 		expect(devices).toHaveProperty('address')
// 		expect(devices).toHaveProperty('family')
// 		expect(devices).toHaveProperty('port')
// 		expect(devices).toHaveProperty('size')
// 	})
// })
